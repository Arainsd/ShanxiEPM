@using hc.epm.UI.Common;
@using hc.epm.DataModel.Basic;
@using hc.Plat.Common.Extend;
@using hc.epm.Common;
@model Base_Protocol

@{
    ViewBag.Title = "Detail";
}

<div class="form-wrap" style="min-height:300px;position: relative;">
    <table class="layui-table" style="width:80%;margin:0 auto;margin-top:20px;table-layout: fixed">
        <tbody>
            <tr>
                <td style="text-align:right;width:25%;">标题</td>
                <td>@Model.Title</td>
            </tr>
            <tr>
                <td style="text-align:right">启用/禁用</td>
                <td>@(Model.IsEnable == true ? "启用" : "禁用")</td>
            </tr>
            <tr>
                <td style="text-align:right">确认状态</td>
                <td>@(Model.IsConfirm == true ? "已确认" : "未确认")</td>
            </tr>
            <tr>
                <td style="text-align:right">协议类型</td>
                <td>@(Model.Type.ToEnumReq<ProtocolType>().GetText())</td>
            </tr>
            <tr>
                <td style="text-align:right">内容</td>
                <td style="word-break: break-all">@Html.Raw(Model.Info)</td>
            </tr>
            <tr>
                <td style="text-align:right">说明</td>
                <td style="word-break: break-all">@Model.Remark</td>
            </tr>
        </tbody>
    </table>
    <div class="layui-form-item row btn-save" style="position: absolute;">
        <input type="button" name="close" value="关&nbsp;&nbsp;&nbsp;闭" class="layui-btn layui-btn-small button-base bg-cancel cancel" />
    </div>
</div>
<script>
    layui.use(['layer', 'form', 'element'], function () {
        var layer = layui.layer, form = layui.form, element = layui.element;
        //关闭弹出iframe层
        $('.cancel').on("click", function () {
            var index = parent.layer.getFrameIndex(window.name); //获取窗口索引
            parent.layer.close(index);//关闭窗口
        });
    });

</script>