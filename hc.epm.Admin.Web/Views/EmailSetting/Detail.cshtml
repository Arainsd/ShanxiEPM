
@using hc.epm.UI.Common;
@using hc.epm.DataModel.Msg
@model Msg_EmailSetting

@{
    ViewBag.Title = "Detail";
}

<div class="form-wrap" style="min-height: 400px;position: relative;">
    <table class="layui-table" style="width:80%;margin:0 auto;margin-top:20px;table-layout: fixed">
        <tbody>
            <tr>
                <td style="text-align:right;width:25%;">模版</td>
                <td>@Model.No</td>
            </tr>
            <tr>
                <td style="text-align:right">模版名称</td>
                <td>@Model.Name</td>
            </tr>
            <tr>
                <td style="text-align:right">邮件接口服务器</td>
                <td>@Model.Servers</td>
            </tr>
            <tr>
                <td style="text-align:right">邮件协议</td>
                <td>@Model.MailProtocol</td>
            </tr>
            <tr>
                <td style="text-align:right">SSL端口号</td>
                <td>@Model.SSL</td>
            </tr>
            <tr>
                <td style="text-align:right">邮件接口用户名</td>
                <td>@Model.UserName</td>
            </tr>
            <tr>
                <td style="text-align:right">邮件接口密码</td>
                <td>@Model.PassWord</td>
            </tr>
            <tr>
                <td style="text-align:right">返回值状态码说明</td>
                <td style="word-break: break-all">@Model.ReturnValDes</td>
            </tr>
            <tr>
                <td style="text-align:right">启用/禁用</td>
                <td>@(Model.IsEnable == true ? "启用" : "禁用")</td>
            </tr>
            <tr>
                <td style="text-align:right">确认状态</td>
                <td>@(Model.IsConfirm == true ? "已确认" : "未确认")</td>
            </tr>

        </tbody>
    </table>
    <div class="layui-form-item row btn-save" style="position: absolute;">
        <input type="button" name="close" value="关&nbsp;&nbsp;&nbsp;闭" class="layui-btn layui-btn-small button-base bg-cancel cancel" />
    </div>
</div>
<script>
    layui.use(['layer', 'form', 'element'], function () {
        var layer = layui.layer, form = layui.form, element = layui.element;
        //关闭弹出iframe层
        $('.cancel').on("click", function () {
            var index = parent.layer.getFrameIndex(window.name); //获取窗口索引
            parent.layer.close(index);//关闭窗口
        });
    });

</script>